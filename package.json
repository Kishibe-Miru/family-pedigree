{
  "name": "family-pedigree",
  "version": "5.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build:engine": "esbuild src/browser/entry.ts --bundle --format=iife --outfile=家族谱系图工具/engine.bundle.js",
    "typecheck": "tsc --noEmit",
    "test": "node --import tsx --test tests/*.test.ts"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "esbuild": "^0.25.0",
    "playwright": "^1.61.1",
    "tsx": "^4.20.0",
    "typescript": "^5.6.0"
  }
}
